

def fn_get_team_details(team_id : int) -> dict:
    # returns team details based on the ID
    return None


def fn_get_elements_details(player_id : int) -> dict:
    # returns player details based on the ID
    return None

def fn_get_elements_summary(player_id : int) -> dict:
    # Returns an element's upcoming fixtures, a history of completed Gameweeks, and a summary of metrics from previous seasons.
    return None


def fn_get_entry_history(manager_id : int) -> dict:
    # Returns an entry's season summary, league and cup standings, budget and transfers made. for all game weeks
    return None


def fn_get_entry_transfers(manager_id : int) -> dict:
    # Returns an entry's transfer transactions for all game weeks
    return None